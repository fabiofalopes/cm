# Quiz #3 - Modelos de Desenvolvimento (Parte 2)

### Pergunta 1

A abordagem **Nativa Pura** tem como principal desvantagem o [ ... ], uma vez que exige o desenvolvimento de uma aplicação para cada sistema operativo (e.g., [ ... ] para iOS e [ ... ] para Android). No entanto, oferece vantagens como [ ... ] máxima e o melhor [ ... ].

Existem duas grandes técnicas na abordagem **Híbrida-Nativa**. A primeira, usada pelo React Native, recorre a um [ ... ] para fazer a ponte com os componentes nativos em tempo de [ ... ]. A segunda, usada por frameworks como Flutter e .NET MAUI, utiliza um [ ... ] que traduz o código para a plataforma final em tempo de [ ... ].

Uma das diferenças fundamentais entre as frameworks Híbrida-Nativa é a forma como desenham a interface. Frameworks como React Native usam widgets [ ... ], que são os componentes gráficos nativos do SO. Já o Flutter usa widgets [ ... ], que são desenhados pela própria framework, garantindo que a aparência é [ ... ] entre plataformas.

A framework Híbrida-Nativa mais popular, tanto em pesquisas Google como em utilização por developers, tem sido o [ ... ], desenvolvido pela Google e que usa a linguagem [ ... ]. A sua principal concorrente é o [ ... ], desenvolvido pelo Facebook.

A capacidade de atualizar uma app sem passar pela store chama-se [ ... ] e é possível em frameworks que usam um JS Engine, como o React Native. 